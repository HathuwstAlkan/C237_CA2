<!DOCTYPE html>
<html>
<head>
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <!-- Root-level view ⇒ use 'partials/navbar' -->
  <%- include('partials/navbar') %>

  <div class="container py-5">
    <h1 class="text-center mb-4">Admin Dashboard</h1>

    <div class="card shadow-sm mx-auto" style="max-width: 900px;">
      <div class="card-header bg-dark text-white text-center">
        Welcome, <%= (user && user.username) ? user.username : 'Admin' %>!
      </div>
      <div class="card-body text-center">
        <p class="mb-3">
          As an administrator, you can manage users and view their information.
        </p>
        <a href="/customers" class="btn btn-primary">View All Users</a>
        <!-- Optional quick links
        <a href="/books" class="btn btn-outline-primary ms-2">Manage Books</a>
        -->
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
